import os

# Get the base directory of the script
base_directory = os.path.dirname(os.path.abspath(__file__))

# Now you can use the base_directory variable to construct paths relative to the script
model_path0 = os.path.join(base_directory, "models", "model20211201-120000")


